schema_version: "WPS-1.0"
work_package_id: "WP-RT-1018"
capability_id: {id: "L6-OPT", version: "1.0"}
title: "Runtime backlog: Layer 6 learning and adaptation loop"
layer: "Layer 6"
description: "Compute calibration weights and episodic embeddings out-of-band."
objective: "Deliver deterministic Brier-based discounting and embedding outputs."
status: "Draft"
planning_status: "READY"
is_immutable: false
owner: "Runtime Planning Board"
governance:
  target_subsystem: "SLS-600"
  traceability:
    implements_req: ["FR-014", "NFR-004"]
    trace_sls: "SLS-600"
    trace_adr: ["ADR-0001"]
    trace_vvc: "TVM-001"
preconditions:
  - {predicate: "capability.complete('L5-EXE')"}
  - {predicate: "capability.complete('L1-RDB')"}
  - {predicate: "capability.complete('L1-MEM')"}
resources:
  cpu: "high"
  memory: "2GB"
  network: false
  filesystem:
    write:
      - "06-runtime/afrp_runtime/layer6/learning.py"
      - "tests/unit/test_layer6.py"
      - "tests/integration/test_layer6_learning_integration.py"
      - "docs/runtime/layer6-learning.md"
      - "05-work-packages/WP-RT-1018/evidence/"
execution:
  priority: "high"
  estimated_complexity: "large"
  estimated_duration: "3d"
  parallelizable: true
  deterministic: true
rollback: {strategy: "git_checkout_bounded_files", restore_tag: "HEAD"}
inputs:
  required_files:
    - "06-runtime/afrp_runtime/layer5/execution.py"
    - "06-runtime/afrp_runtime/layer1/memory.py"
    - "06-runtime/afrp_runtime/layer1/persistence.py"
outputs:
  expected_source_files:
    - "06-runtime/afrp_runtime/layer6/learning.py"
    - "tests/unit/test_layer6.py"
    - "tests/integration/test_layer6_learning_integration.py"
    - "docs/runtime/layer6-learning.md"
  expected_evidence: ["05-work-packages/WP-RT-1018/evidence/EXEC-118.yaml"]
produces:
  capability: {id: "L6-OPT", version: "1.0"}
scope:
  bounded_files:
    - "06-runtime/afrp_runtime/layer6/learning.py"
    - "tests/unit/test_layer6.py"
    - "tests/integration/test_layer6_learning_integration.py"
    - "docs/runtime/layer6-learning.md"
requirements:
  acceptance_criteria:
    - "Brier scoring and weight updates are deterministic and bounded."
    - "CIO-11 and CIO-12 outputs are schema-valid and reproducible."
    - "Learning path is explicitly out-of-band and does not bypass policy controls."
  required_tests:
    - "Unit: calibration math, bounds, embedding determinism."
    - "Integration: execution history to CIO-11/CIO-12 outputs."
  evidence_requirements:
    - "ERS-1.0 evidence with calibration metrics and reproducibility checks."
quality_gates:
  ruff_lint: {required: true, command: "uv run ruff check 06-runtime/ tests/"}
  mypy_typecheck: {required: true, command: "uv run mypy --strict 06-runtime/ tests/"}
  pytest_units: {required: true, command: "uv run pytest tests/unit/test_layer6.py -v"}
  pytest_integration: {required: true, command: "uv run pytest tests/integration/test_layer6_learning_integration.py -v"}
  coverage: {required: true, command: "uv run pytest tests --cov --cov-report=term"}
  fit_004: {required: true, command: "uv run afrp validate"}
  boundary_validation: {required: true, command: "uv run afrp evidence --wp WP-RT-1018"}
completion:
  success_requires: ["All acceptance criteria and quality gates pass."]
failure_modes:
  ERR-CALIBRATION: "Calibration update violated numerical or schema constraints."
  ERR-EMBEDDING: "Embedding generation failed determinism constraints."
architecture_references: ["02-architecture/110_RUNTIME_ARCHITECTURE.md#1"]
traceability_links: ["FR-014", "NFR-004", "FIT-004", "FIT-005", "SLS-600"]
execution_prompt: |
  Execute WP-RT-1018 strictly within bounded_files.
