schema_version: "WPS-1.0"
work_package_id: "WP-RT-1016"
capability_id: {id: "L4-VAL", version: "1.0"}
title: "Runtime backlog: Layer 4 policy validation engine"
layer: "Layer 4"
description: "Validate CIO-06 against policy constraints and emit CIO-07."
objective: "Deliver deterministic policy projection with safe null-action fallback."
status: "Draft"
planning_status: "READY"
is_immutable: false
owner: "Runtime Planning Board"
governance:
  target_subsystem: "SLS-402"
  traceability:
    implements_req: ["FR-012"]
    trace_sls: "SLS-402"
    trace_adr: ["ADR-0001"]
    trace_vvc: "TVM-001"
preconditions: [{predicate: "capability.complete('L4-DEC')"}]
resources:
  cpu: "medium"
  memory: "1GB"
  network: false
  filesystem:
    write:
      - "06-runtime/afrp_runtime/layer4/policy.py"
      - "tests/unit/test_layer4_policy.py"
      - "tests/integration/test_layer4_policy_integration.py"
      - "docs/runtime/layer4-policy.md"
      - "05-work-packages/WP-RT-1016/evidence/"
execution:
  priority: "critical"
  estimated_complexity: "large"
  estimated_duration: "3d"
  parallelizable: false
  deterministic: true
rollback: {strategy: "git_checkout_bounded_files", restore_tag: "HEAD"}
inputs:
  required_files:
    - "06-runtime/afrp_runtime/layer4/optimizer.py"
    - "06-runtime/afrp_runtime/contracts/cio.py"
outputs:
  expected_source_files:
    - "06-runtime/afrp_runtime/layer4/policy.py"
    - "tests/unit/test_layer4_policy.py"
    - "tests/integration/test_layer4_policy_integration.py"
    - "docs/runtime/layer4-policy.md"
  expected_evidence: ["05-work-packages/WP-RT-1016/evidence/EXEC-116.yaml"]
produces:
  capability: {id: "L4-VAL", version: "1.0"}
  unlocks: [{id: "L5-EXE", version: "1.0"}]
scope:
  bounded_files:
    - "06-runtime/afrp_runtime/layer4/policy.py"
    - "tests/unit/test_layer4_policy.py"
    - "tests/integration/test_layer4_policy_integration.py"
    - "docs/runtime/layer4-policy.md"
requirements:
  acceptance_criteria:
    - "Policy engine enforces Pi_C projection and null-action fallback."
    - "Mission-profile constraints are explicit and auditable."
    - "CIO-07 outputs include complete policy provenance."
  required_tests:
    - "Unit: per-rule policy checks and rejection handling."
    - "Integration: CIO-06 to CIO-07 authorized/rejected branches."
  evidence_requirements:
    - "ERS-1.0 evidence with policy decision audit traces."
quality_gates:
  ruff_lint: {required: true, command: "uv run ruff check 06-runtime/ tests/"}
  mypy_typecheck: {required: true, command: "uv run mypy --strict 06-runtime/ tests/"}
  pytest_units: {required: true, command: "uv run pytest tests/unit/test_layer4_policy.py -v"}
  pytest_integration: {required: true, command: "uv run pytest tests/integration/test_layer4_policy_integration.py -v"}
  coverage: {required: true, command: "uv run pytest tests --cov --cov-report=term"}
  fit_004: {required: true, command: "uv run afrp validate"}
  boundary_validation: {required: true, command: "uv run afrp evidence --wp WP-RT-1016"}
completion:
  success_requires: ["All acceptance criteria and quality gates pass."]
failure_modes:
  ERR-POLICY-PROJECTION: "Policy projection failed or produced inconsistent authorization."
architecture_references: ["02-architecture/110_RUNTIME_ARCHITECTURE.md#1"]
traceability_links: ["FR-012", "FIT-004", "FIT-005", "SLS-402"]
execution_prompt: |
  Execute WP-RT-1016 strictly within bounded_files.
