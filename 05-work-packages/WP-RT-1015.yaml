schema_version: "WPS-1.0"
work_package_id: "WP-RT-1015"
capability_id: {id: "L4-DEC", version: "1.0"}
title: "Runtime backlog: Layer 4 utility optimizer"
layer: "Layer 4"
description: "Optimize CIO-05B contexts into CIO-06 execution candidates."
objective: "Deliver deterministic utility maximization under defined constraints."
status: "Draft"
planning_status: "READY"
is_immutable: false
owner: "Runtime Planning Board"
governance:
  target_subsystem: "SLS-401"
  traceability:
    implements_req: ["FR-012", "NFR-008"]
    trace_sls: "SLS-401"
    trace_adr: ["ADR-0001"]
    trace_vvc: "TVM-001"
preconditions: [{predicate: "capability.complete('L4-FUS')"}]
resources:
  cpu: "high"
  memory: "2GB"
  network: false
  filesystem:
    write:
      - "06-runtime/afrp_runtime/layer4/optimizer.py"
      - "tests/unit/test_layer4_optimizer.py"
      - "tests/integration/test_layer4_optimizer_integration.py"
      - "docs/runtime/layer4-optimizer.md"
      - "05-work-packages/WP-RT-1015/evidence/"
execution:
  priority: "critical"
  estimated_complexity: "large"
  estimated_duration: "3d"
  parallelizable: false
  deterministic: true
rollback: {strategy: "git_checkout_bounded_files", restore_tag: "HEAD"}
inputs: {required_files: ["06-runtime/afrp_runtime/layer4/synthesizer.py"]}
outputs:
  expected_source_files:
    - "06-runtime/afrp_runtime/layer4/optimizer.py"
    - "tests/unit/test_layer4_optimizer.py"
    - "tests/integration/test_layer4_optimizer_integration.py"
    - "docs/runtime/layer4-optimizer.md"
  expected_evidence: ["05-work-packages/WP-RT-1015/evidence/EXEC-115.yaml"]
produces:
  capability: {id: "L4-DEC", version: "1.0"}
  unlocks: [{id: "L4-VAL", version: "1.0"}]
scope:
  bounded_files:
    - "06-runtime/afrp_runtime/layer4/optimizer.py"
    - "tests/unit/test_layer4_optimizer.py"
    - "tests/integration/test_layer4_optimizer_integration.py"
    - "docs/runtime/layer4-optimizer.md"
requirements:
  acceptance_criteria:
    - "Optimizer deterministically selects argmax U_r candidate under model constraints."
    - "CIO-06 output is schema-valid and reproducible."
  required_tests:
    - "Unit: objective function, constraint handling, edge/corner cases."
    - "Integration: CIO-05B to CIO-06 pipeline."
  evidence_requirements:
    - "ERS-1.0 evidence with deterministic optimizer oracles."
quality_gates:
  ruff_lint: {required: true, command: "uv run ruff check 06-runtime/ tests/"}
  mypy_typecheck: {required: true, command: "uv run mypy --strict 06-runtime/ tests/"}
  pytest_units: {required: true, command: "uv run pytest tests/unit/test_layer4_optimizer.py -v"}
  pytest_integration: {required: true, command: "uv run pytest tests/integration/test_layer4_optimizer_integration.py -v"}
  coverage: {required: true, command: "uv run pytest tests --cov --cov-report=term"}
  fit_004: {required: true, command: "uv run afrp validate"}
  boundary_validation: {required: true, command: "uv run afrp evidence --wp WP-RT-1015"}
completion:
  success_requires: ["All acceptance criteria and quality gates pass."]
failure_modes:
  ERR-OPTIMIZATION: "Utility optimization failed or produced invalid candidate."
architecture_references: ["02-architecture/110_RUNTIME_ARCHITECTURE.md#1"]
traceability_links: ["FR-012", "NFR-008", "FIT-004", "FIT-005", "SLS-401"]
execution_prompt: |
  Execute WP-RT-1015 strictly within bounded_files.
