schema_version: "WPS-1.0"
work_package_id: "WP-IMP-0015"
capability_id:
  id: "L1-RDB"
  version: "1.0"
title: "Implement L1 relational persistence ledger"
status: "Completed"
is_immutable: true

governance:
  target_subsystem: "SLS-100"
  traceability:
    implements_req: ["FR-009", "NFR-005"]
    trace_sls: "SLS-100"
    trace_adr: ["ADR-0001"]
    trace_vvc: "TVM-001"

preconditions:
  - predicate: "capability.complete('L1-ING')"

resources:
  cpu: "low"
  memory: "512MB"
  network: false
  filesystem:
    write:
      - "06-runtime/afrp_runtime/layer1/persistence.py"
      - "tests/unit/test_layer1.py"
      - "05-work-packages/WP-IMP-0015/evidence/"
    read:
      - "06-runtime/afrp_runtime/contracts/"

execution:
  priority: "high"
  estimated_complexity: "small"
  estimated_duration: "1h"
  parallelizable: true
  deterministic: true

rollback:
  strategy: "git_checkout_bounded_files"
  restore_tag: "HEAD"

inputs:
  required_files:
    - "06-runtime/afrp_runtime/contracts/cio.py"

outputs:
  expected_source_files:
    - "06-runtime/afrp_runtime/layer1/persistence.py"
  expected_evidence:
    - "05-work-packages/WP-IMP-0015/evidence/EXEC-013.yaml"

execution_results:
  source_files_modified:
    - "06-runtime/afrp_runtime/layer1/persistence.py"
    - "tests/unit/test_layer1.py"
  evidence_generated:
    - "05-work-packages/WP-IMP-0015/evidence/EXEC-013.yaml"

produces:
  capability:
    id: "L1-RDB"
    version: "1.0"
  unlocks:
    - id: "L5-EXE"
      version: "1.0"
    - id: "L6-OPT"
      version: "1.0"

scope:
  bounded_files:
    - "06-runtime/afrp_runtime/layer1/persistence.py"
    - "tests/unit/test_layer1.py"
  non_goals:
    - "No belief formation (Layer 2)."

requirements:
  implementation:
    module: "06-runtime/afrp_runtime/layer1/persistence.py"
    rules:
      - "Append-only SQLite with synchronous=FULL (RPO=0)."
      - "Replay by sequence range for FIT-008."
      - "Order-event audit trail for NFR-007."

quality_gates:
  ruff_lint:
    required: true
    command: "uv run ruff check 06-runtime/ tests/"
  mypy_typecheck:
    required: true
    command: "uv run mypy --strict 06-runtime/ tests/unit/test_layer1.py"
  pytest_units:
    required: true
    command: "uv run pytest tests/unit/test_layer1.py -v"

completion:
  success_requires:
    - "Contract-only interconnect; gates pass with zero errors."

failure_modes:
  ERR-CONTRACT-VIOLATION: "Malformed telemetry or payload rejected."

execution_prompt: |
  Execute WP-IMP-0015 strictly within bounded_files.
